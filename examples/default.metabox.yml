version: 0.1
workspace:
    root_path: /tmp
    cache_path: ./cache
    versions_path: ./versions.txt
    hooks:
        pre_backup:
            - "echo start"
        post_backup:
            - "echo end"
    options:
        compress: .tar.gz
        hash: md5
target:
    driver: local
    local:
        prefix_path: /home/nmcapule/projects/metabox-go
        includes:
            - "**/*"
        excludes:
            - ".git/"
            - "dockerdata/"
            - "vendor/"
            - "**/*.exe"
backup:
    driver: s3
    s3:
        access_key_id: sample-access-key
        secret_access_key: sample-secret-key
        region: ap-southeast-1
        bucket: my-bucket
        prefix_path: archives
